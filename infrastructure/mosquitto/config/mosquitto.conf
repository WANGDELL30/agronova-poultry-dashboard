listener 1883
protocol mqtt

# Phase 1 local-development configuration. Authentication will be added before
# the broker is exposed outside a trusted development machine.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest stdout
connection_messages true
